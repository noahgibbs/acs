<%= form_for(@room_state) do |f| %>
  <% if @room_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_state.errors.count, "error") %> prohibited this room_state from being saved:</h2>

      <ul>
      <% @room_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :json_data %><br />
    <%= f.text_area :json_data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
